# Architecture decision records

Architecture decision records document accepted choices that materially affect evidence semantics, public interfaces,
security boundaries, storage, or operations.

Use sequential names such as `0001-short-decision-title.md`. Each record should include status, context, decision,
consequences, alternatives, and links to the implementing pull request. Records are immutable after acceptance except
for small corrections; a later decision supersedes an earlier one.
